import { FC } from 'react'
import { Formula } from '../../../components/Formula'

const FormulaDemo: FC = () => (
  <Formula content="\forall x \in X, \quad \exists y \leq \epsilon" />
)

export default FormulaDemo
